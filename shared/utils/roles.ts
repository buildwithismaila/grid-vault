// Role definitions have been moved to the seed file (server/db/seeds/rbac.ts).
// All roles are equal — there is no system/custom distinction.
// Roles are managed entirely via the admin UI and stored in the `role` table.
